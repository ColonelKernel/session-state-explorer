# Placeholder

This file keeps `data/examples/` present in version control and documents where example
inputs live.

- The committed example project is [`example_project.rpp`](example_project.rpp).
- Generated audio stems are written to `audio/` by
  [`make_example_data.py`](make_example_data.py) and are intentionally git-ignored.

See [README.md](README.md) in this folder for details.
